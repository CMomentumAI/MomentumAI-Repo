# Momentum — environment variables
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# In Railway, set these as environment variables in the project dashboard.
# Every variable listed here (except ELEVENLABS_VOICE_ID) is REQUIRED.

# ─── Authentication ─────────────────────────────────────────────────────────────
# Min 32 characters. Generate with:
#   openssl rand -base64 32
JWT_SECRET=

# ─── PostgreSQL (OMI realtime transcript storage) ─────────────────────────────
# Railway Postgres / hosted Postgres connection string.
DATABASE_URL=

# ─── AWS S3 ─────────────────────────────────────────────────────────────────────
# Create a private S3 bucket in your AWS account.
# The IAM user needs: s3:GetObject, s3:PutObject, s3:DeleteObject, s3:ListBucket
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=momentum-patient-data

# ─── AI Services ────────────────────────────────────────────────────────────────
# Perplexity — appointment transcript summarization
# Get key at: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=

# Google Gemini — RAG embedding index + chat + form auto-fill
# Get key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# ElevenLabs — text-to-speech audio generation
# Get key at: https://elevenlabs.io/settings/api-keys
ELEVENLABS_API_KEY=

# Optional: pin a specific ElevenLabs voice ID.
# Defaults to "Rachel" (voice ID: 21m00Tcm4TlvDq8ikWAM) if not set.
ELEVENLABS_VOICE_ID=

# ─── OMI Wearable Webhook ───────────────────────────────────────────────────────
# HMAC-SHA256 secret shared with the OMI device cloud.
# Min 32 characters. Generate with:
#   openssl rand -base64 32
OMI_WEBHOOK_SECRET=

# ─── Cross-origin / CORS (Vercel frontend <-> Railway backend) ──────────────────
# Required in Railway so the browser allows the Vercel frontend to call this API.
# Leave empty for local dev (localhost origins are always allowed).
#
# Production — set to your canonical Vercel URL (no trailing slash):
#   FRONTEND_URL=https://momentum.vercel.app
#
# Preview deployments — comma-separated list of additional allowed origins:
#   ADDITIONAL_ORIGINS=https://momentum-pr-42.vercel.app,https://staging.example.com
FRONTEND_URL=
ADDITIONAL_ORIGINS=
